#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() {

    // scope
    float a, b;
    float ans;

    // masukkan
    cin >> a >> b;

    // keluaran
    cout << fixed << setprecision(2) << (a*b)/2 << endl;
    
    cin.get();
    return 0;
}