#include <iostream>
using namespace std;

int main() {

    // scope
    int a;

    // masukan
    cin >> a;

    // if/else
    if (a > 0) {
        cout << a;
    } else {
        
    }
    
    cin.get();
    return 0;
}