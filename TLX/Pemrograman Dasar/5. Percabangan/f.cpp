#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() {

    // scope
    float a;

    // masukan
    cin >> a;

    // keluaran
    cout << floor(a) << " " << ceil(a);

    cin.get();
    return 0;
}